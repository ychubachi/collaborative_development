language: ruby

before_install:
  - gem update --system
  - gem install bundler

after_success:
  - git conifg --local user.name $GH_USER_NAME
  - git conifg --local user.email $GH_USER_EMAIL
  - cd docs/
  - git init
  - git status
  - git add .
  - git status
  - git commit -m "Publishing site on `date "+%Y-%m-%d %H:%M:%S"`"
  - git status
  - git push https://$GH_USER_NAME:$GH_TOKEN@$GH_REF master:gh-pages